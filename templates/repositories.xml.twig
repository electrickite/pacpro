<?xml version="1.0" encoding="UTF-8"?>
<repositories total="{{ repositories | length }}" page="1" of="1">
  {% for repo in repositories %}
    {% include '_repository.xml.twig' %}
  {% endfor %}
</repositories>
